
/**
 * @param {number} x
 *        The angle to convert, in gradians.
 * @return  {number}
 *        The argument `x`, in radians.
 */
export function gradiansToRadians(x: number): number {
  return x * (Math.PI / 200);
}
