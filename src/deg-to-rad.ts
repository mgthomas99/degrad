
/**
 * @param {number} x
 *        The angle to convert, in degrees.
 * @return  {number}
 *        The argument `x`, in radians.
 */
export function degreesToRadians(x: number): number {
  return x * (Math.PI / 180);
}
